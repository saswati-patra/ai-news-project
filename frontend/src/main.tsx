import { createRoot } from "react-dom/client";

import { SummariesPage } from "@/pages/summaries-page";
import "./styles.css";

createRoot(document.getElementById("root")!).render(<SummariesPage />);
